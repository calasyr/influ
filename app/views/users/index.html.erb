<p id="notice"><%= notice %></p>

<h1>The 20 Most Influential Reddit Users</h1>
<h3><span>/r</span><span>/NetflixBestOf</span><span>/comments</span><span>/7p8s2b/</span><span>us_the_prestige_2006_desperate_to_reveal_each/</span></h3>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.comment_count %></td>
        <td><%= user.reply_count %></td>
      </tr>
    <% end %>
  </tbody>
</table>

